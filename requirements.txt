requests>=2.31.0
curl_cffi>=0.15.0  # >=0.15.0 required: its impersonated ClientHello survives the proxy CONNECT, which is what now beats Legacy.com's Cloudflare TLS fingerprinting (see utils/rate_limiter.create_session)
beautifulsoup4>=4.12.0
lxml>=5.0.0
mysql-connector-python>=8.3.0
python-dotenv>=1.0.0
boto3>=1.34.0
Pillow>=10.0.0
